package com.microfinance.api_gateway.config;

import org.springdoc.core.models.GroupedOpenApi;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class GatewayConfig {

/*


    @Bean
    public GroupedOpenApi authentificationServicesApi() {
        return GroupedOpenApi.builder()
                .group("authentification-services")
                .pathsToMatch("/auth/**")
                .build();
    }


    @Bean
    public GroupedOpenApi userServicesApi() {
        return GroupedOpenApi.builder()
                .group("users-services")
                .pathsToMatch("/users/**")
                .build();
    }

    @Bean
    public GroupedOpenApi transactionServicesApi() {
        return GroupedOpenApi.builder()
                .group("transaction-services")
                .pathsToMatch("/transactions/**")
                .build();
    }
    @Bean
    public GroupedOpenApi reportingServicesApi() {
        return GroupedOpenApi.builder()
                .group("reporting-services")
                .pathsToMatch("/reports/**")
                .build();
    }

    @Bean
    public GroupedOpenApi loanServicesApi() {
        return GroupedOpenApi.builder()
                .group("loans-services")
                .pathsToMatch("/loan/**")
                .build();
    }
    @Bean
    public GroupedOpenApi clientServicesApi() {
        return GroupedOpenApi.builder()
                .group("client-services")
                .pathsToMatch("/client/**")
                .build();
    }

*/

}
